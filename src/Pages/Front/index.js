import { connect } from 'react-redux'
import FrontView from './View';

const mapStateToProps = (state) => {
    return {
    }
}

const mapDispatchToProps = (dispatch) => {
    return {
    }
}

const Front = connect(
    mapStateToProps,
    mapDispatchToProps
)(FrontView)

export default Front
